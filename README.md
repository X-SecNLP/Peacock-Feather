# Peacock Feather

1. Copy the `code.el`.

2. In Emacs, paste it into a buffer (e.g., `*scratch*`).

3. Run `M-x eval-buffer`.

4. Start the simulation by typing `M-x semantic-niche-start`.

5. Enter your target word (e.g., `evolution`).
